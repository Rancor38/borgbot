const help = require("./help.json")
const overdrive = require("./overdrive.json")
const reset = require("./resetPrompt.json")
const foodData = require("./foodData.json")
const validCommands = require("./validCommands.json")

module.exports = {
    help,
    overdrive,
    reset,
    foodData,
    validCommands,
}
